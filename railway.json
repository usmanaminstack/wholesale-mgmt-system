{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd backend && npm install --production && node index.js",
        "healthcheckPath": "/",
        "healthcheckTimeout": 120
    }
}