# WARNING: Do not use the .internal Railway URL here (e.g. http://wholesale-mgmt-system.railway.internal/api).
# The frontend runs in the user's browser (client-side) and needs a PUBLIC URL to access the API.
# Generate a public domain in Railway (Service Settings -> Domains -> Generate Domain) and replace the placeholder below.
VITE_API_BASE_URL=https://wholesale-mgmt-system-production-bfd5.up.railway.app/api
